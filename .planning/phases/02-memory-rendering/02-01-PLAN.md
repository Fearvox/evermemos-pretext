---
phase: 2
plan: 01
wave: 1
depends_on: []
files_modified:
  - shared/types/memory.ts
  - shared/types/pagination.ts
  - shared/types/index.ts
  - hub/package.json
  - hub/src/lib/pretext/shrinkwrap.ts
  - hub/src/lib/pretext/index.ts
requirements_addressed: [MEM-06, MEM-05]
autonomous: true
---

# Plan 02-01: Zod Schemas, Shared Types, and Pretext Shrinkwrap Utilities

<objective>
Create the `shared/` type system with Zod schemas for Episode, Profile, and KnowledgeEntry (including multilingual test shapes), the cursor-based pagination envelope, and port Pretext's shrinkwrap utilities (`collectWrapMetrics`, `findTightWrapMetrics`) into hub for bubble layout.
</objective>

<must_haves>
- Zod schemas for Episode, Profile, KnowledgeEntry with inferred TypeScript types exported
- Pagination envelope type `PaginatedResponse<T>` with `data`, `cursor`, `hasMore` fields
- `collectWrapMetrics` and `findTightWrapMetrics` functions in `hub/src/lib/pretext/shrinkwrap.ts`
- All types importable from `shared/types` barrel
- `zod` installed in hub/package.json
</must_haves>

## Tasks

<task id="02-01-T1">
<name>Install zod dependency</name>
<read_first>
- hub/package.json
</read_first>
<action>
Run `cd hub && npm install zod` to add Zod as a dependency.
</action>
<acceptance_criteria>
- `hub/package.json` contains `"zod"` in dependencies
</acceptance_criteria>
</task>

<task id="02-01-T2">
<name>Create shared/types/memory.ts with Zod schemas</name>
<read_first>
- .planning/phases/02-memory-rendering/02-CONTEXT.md (D-06, D-07)
- .planning/phases/02-memory-rendering/02-RESEARCH.md (Section 1, Section 4)
</read_first>
<action>
Create `shared/types/memory.ts` with these Zod schemas and inferred types:

```ts
import { z } from 'zod'

// --- Shared enums ---
export const MemoryTypeSchema = z.enum(['episode', 'profile', 'knowledge'])
export type MemoryType = z.infer<typeof MemoryTypeSchema>

// --- Episode ---
export const ConversationTurnSchema = z.object({
  id: z.string(),
  role: z.enum(['user', 'agent']),
  content: z.string(),
  timestamp: z.string().datetime(),
})
export type ConversationTurn = z.infer<typeof ConversationTurnSchema>

export const EpisodeSchema = z.object({
  id: z.string(),
  type: z.literal('episode'),
  title: z.string(),
  turns: z.array(ConversationTurnSchema),
  tags: z.array(z.string()),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
})
export type Episode = z.infer<typeof EpisodeSchema>

// --- Profile ---
export const ProfileFieldSchema = z.object({
  label: z.string(),
  value: z.string(),
})
export type ProfileField = z.infer<typeof ProfileFieldSchema>

export const ProfileSchema = z.object({
  id: z.string(),
  type: z.literal('profile'),
  name: z.string(),
  role: z.string(),
  description: z.string(),
  fields: z.array(ProfileFieldSchema),
  tags: z.array(z.string()),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
})
export type Profile = z.infer<typeof ProfileSchema>

// --- Knowledge ---
export const KnowledgeEntrySchema = z.object({
  id: z.string(),
  type: z.literal('knowledge'),
  subject: z.string(),
  content: z.string(),
  tags: z.array(z.string()),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
})
export type KnowledgeEntry = z.infer<typeof KnowledgeEntrySchema>

// --- Union ---
export const MemoryItemSchema = z.discriminatedUnion('type', [
  EpisodeSchema,
  ProfileSchema,
  KnowledgeEntrySchema,
])
export type MemoryItem = z.infer<typeof MemoryItemSchema>
```
</action>
<acceptance_criteria>
- File `shared/types/memory.ts` exists
- Contains `export const EpisodeSchema`
- Contains `export const ProfileSchema`
- Contains `export const KnowledgeEntrySchema`
- Contains `export const MemoryItemSchema`
- Contains `export type MemoryItem`
- Contains `z.discriminatedUnion('type'`
</acceptance_criteria>
</task>

<task id="02-01-T3">
<name>Create shared/types/pagination.ts</name>
<read_first>
- .planning/phases/02-memory-rendering/02-RESEARCH.md (Section 7 — cursor-based pagination)
</read_first>
<action>
Create `shared/types/pagination.ts`:

```ts
import { z, type ZodType } from 'zod'

export function paginatedResponseSchema<T extends ZodType>(itemSchema: T) {
  return z.object({
    data: z.array(itemSchema),
    cursor: z.string().nullable(),
    hasMore: z.boolean(),
    total: z.number().int().nonneg().optional(),
  })
}

export type PaginatedResponse<T> = {
  data: T[]
  cursor: string | null
  hasMore: boolean
  total?: number
}
```
</action>
<acceptance_criteria>
- File `shared/types/pagination.ts` exists
- Contains `export function paginatedResponseSchema`
- Contains `export type PaginatedResponse`
- Contains `cursor: z.string().nullable()`
- Contains `hasMore: z.boolean()`
</acceptance_criteria>
</task>

<task id="02-01-T4">
<name>Create shared/types/index.ts barrel</name>
<read_first>
- shared/types/memory.ts (after T2)
- shared/types/pagination.ts (after T3)
</read_first>
<action>
Create `shared/types/index.ts`:

```ts
export {
  MemoryTypeSchema,
  type MemoryType,
  ConversationTurnSchema,
  type ConversationTurn,
  EpisodeSchema,
  type Episode,
  ProfileFieldSchema,
  type ProfileField,
  ProfileSchema,
  type Profile,
  KnowledgeEntrySchema,
  type KnowledgeEntry,
  MemoryItemSchema,
  type MemoryItem,
} from './memory'

export {
  paginatedResponseSchema,
  type PaginatedResponse,
} from './pagination'
```
</action>
<acceptance_criteria>
- File `shared/types/index.ts` exists
- Contains re-exports from `'./memory'` and `'./pagination'`
</acceptance_criteria>
</task>

<task id="02-01-T5">
<name>Port shrinkwrap utilities from Pretext demo</name>
<read_first>
- pretext/pages/demos/bubbles-shared.ts (collectWrapMetrics, findTightWrapMetrics signatures)
- hub/src/lib/pretext/fonts.ts (PRETEXT_LINE_HEIGHT)
- hub/src/lib/pretext/index.ts (existing exports)
</read_first>
<action>
Create `hub/src/lib/pretext/shrinkwrap.ts`:

```ts
// hub/src/lib/pretext/shrinkwrap.ts
// CLIENT ONLY — uses Pretext APIs that require browser Canvas
//
// Ported from pretext/pages/demos/bubbles-shared.ts.
// These are pure functions (not hooks) — they take a PreparedTextWithSegments
// and return metrics. Safe to call from useEffect/useMemo after prepare() resolves.

import {
  layout,
  walkLineRanges,
  type PreparedTextWithSegments,
} from '@pretext'
import { PRETEXT_LINE_HEIGHT } from './fonts'

export type WrapMetrics = {
  lineCount: number
  height: number
  maxLineWidth: number
}

/**
 * Walk all lines at `maxWidth` and return the widest line width,
 * total line count, and total height.
 */
export function collectWrapMetrics(
  prepared: PreparedTextWithSegments,
  maxWidth: number,
  lineHeight: number = PRETEXT_LINE_HEIGHT,
): WrapMetrics {
  let maxLineWidth = 0
  const lineCount = walkLineRanges(prepared, maxWidth, (line) => {
    if (line.width > maxLineWidth) maxLineWidth = line.width
  })
  return {
    lineCount,
    height: lineCount * lineHeight,
    maxLineWidth,
  }
}

/**
 * Binary-search for the tightest container width that does not increase
 * the line count beyond what `maxWidth` produces. Returns metrics at
 * that tight width.
 *
 * This is the core of bubble shrinkwrap: the bubble is as narrow as
 * possible without adding extra line breaks.
 */
export function findTightWrapMetrics(
  prepared: PreparedTextWithSegments,
  maxWidth: number,
  lineHeight: number = PRETEXT_LINE_HEIGHT,
): WrapMetrics {
  const initial = collectWrapMetrics(prepared, maxWidth, lineHeight)
  let lo = 1
  let hi = Math.max(1, Math.ceil(maxWidth))

  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2)
    const midLineCount = layout(prepared, mid, lineHeight).lineCount
    if (midLineCount <= initial.lineCount) {
      hi = mid
    } else {
      lo = mid + 1
    }
  }

  return collectWrapMetrics(prepared, lo, lineHeight)
}
```

Then add exports to `hub/src/lib/pretext/index.ts`:

```ts
export { collectWrapMetrics, findTightWrapMetrics, type WrapMetrics } from './shrinkwrap'
```
</action>
<acceptance_criteria>
- File `hub/src/lib/pretext/shrinkwrap.ts` exists
- Contains `export function collectWrapMetrics`
- Contains `export function findTightWrapMetrics`
- Contains `export type WrapMetrics`
- Contains `import { layout, walkLineRanges` from `'@pretext'`
- `hub/src/lib/pretext/index.ts` contains `export { collectWrapMetrics, findTightWrapMetrics`
</acceptance_criteria>
</task>

## Verification

<verify>
cd hub && grep -q '"zod"' package.json && echo "PASS: zod installed"
test -f ../shared/types/memory.ts && echo "PASS: memory.ts exists"
test -f ../shared/types/pagination.ts && echo "PASS: pagination.ts exists"
test -f ../shared/types/index.ts && echo "PASS: types barrel exists"
test -f src/lib/pretext/shrinkwrap.ts && echo "PASS: shrinkwrap.ts exists"
grep -q 'findTightWrapMetrics' src/lib/pretext/index.ts && echo "PASS: shrinkwrap exported from barrel"
grep -q 'MemoryItemSchema' ../shared/types/memory.ts && echo "PASS: MemoryItemSchema defined"
grep -q 'PaginatedResponse' ../shared/types/pagination.ts && echo "PASS: PaginatedResponse defined"
cd hub && npx tsc --noEmit && echo "PASS: type check"
</verify>
