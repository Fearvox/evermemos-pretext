{
  "installCommand": "cd pretext && npm install --ignore-scripts && npm run build:package && cd ../hub && npm install",
  "buildCommand": "cd hub && npm run build",
  "outputDirectory": "hub/.next",
  "framework": null
}
