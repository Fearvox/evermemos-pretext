{
  "installCommand": "npm install && cd hub && npm install && ln -sf $(pwd)/node_modules ../shared/node_modules",
  "buildCommand": "cd pretext && npx -p typescript@6.0.2 -y tsc -p tsconfig.build.json && cd ../hub && npm run build",
  "outputDirectory": "hub/.next",
  "framework": "nextjs",
  "git": {
    "submodules": true
  }
}
